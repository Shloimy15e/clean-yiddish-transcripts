# Flask Configuration
FLASK_DEBUG=false
FLASK_ENV=production
PORT=5050

# Google Drive API Configuration
# To use Google Drive integration:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Drive API
# 4. Create OAuth 2.0 credentials (Desktop app)
# 5. Download the credentials JSON file
# 6. Rename it to 'credentials.json' and place it in the root directory
# Note: The credentials.json file should NOT be committed to version control
# Note: A token.json file will be automatically generated after first authentication

# Optional: Custom paths for credentials (defaults shown)
# GOOGLE_CREDENTIALS_PATH=credentials.json
# GOOGLE_TOKEN_PATH=token.json

# Application Configuration
# Maximum file size for uploads (in bytes, default: 16MB)
MAX_CONTENT_LENGTH=16777216

# Temporary folder paths (defaults shown)
# UPLOAD_FOLDER=uploads
# TEMP_FOLDER=temp
